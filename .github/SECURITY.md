# 安全策略

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。** 公开的漏洞报告会在修复发布前让所有使用者
暴露于风险中。

请通过以下任一私密渠道报告：

- GitHub Security Advisory（推荐）：在对应仓库的 **Security → Report a vulnerability**
- 邮件：`flare1522@163.com`

报告请尽量包含：

- 受影响的仓库与版本（或 commit）
- 漏洞类型与影响面（能做什么：读取他人数据？冒充身份？拒绝服务？）
- 复现步骤或 PoC
- 你希望的署名方式（或选择匿名）

## 响应流程

| 阶段 | 目标时限 |
|---|---|
| 确认收到 | 3 个工作日内 |
| 初步评估与定级 | 7 个工作日内 |
| 修复并发布 | 视严重程度，高危优先 |
| 公开披露 | 修复发布后，与报告者协商时机 |

我们不运行付费漏洞赏金计划。有效报告会在安全公告中致谢（除非你希望匿名）。

## 支持范围

安全修复只提供给**最新发布版本**。请在报告前确认问题在最新版仍然存在。

## 范围内 / 范围外

**范围内**：本组织下各仓库的源码、发布的包（crates.io / npm / pub.dev）、
默认配置带来的安全弱点。

**范围外**：

- 需要物理接触设备或已被攻破的宿主环境才能利用的问题
- 仅影响示例应用（`examples/*`）中明确标注为演示用途的配置
- 社会工程学、针对项目成员的攻击
- 第三方依赖的已知漏洞（请直接报给上游；但如果我们的用法放大了其影响，欢迎报告）

## 部署方须自行负责的部分

以下不是本项目的漏洞，但是最常见的实际事故来源，部署时请自查：

- **密钥管理**：JWT 签名密钥、网关 token secret、数据库口令必须由环境变量或密钥
  管理服务注入，**不要写进配置文件并提交到版本控制**。
- **传输加密**：生产环境必须启用 TLS。
- **租户隔离**：多租户部署时确认 `tenant_id` 在所有入口都被强制校验。
