name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  check:
    name: fmt / clippy / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 本项目是多仓库结构，各仓的 path 依赖指向同级仓库目录。
      # 单仓 checkout 后这些路径不存在，cargo 会直接解析失败，
      # 所以这里按开发时的布局把依赖仓拉到同级位置。
      - name: 拉取同级仓库
        run: |
          BR="${GITHUB_REF_NAME:-main}"
          clone() {
            git clone --depth 1 -b "$BR" "$1" "$2" 2>/dev/null \
              || git clone --depth 1 "$1" "$2"
          }
          clone https://github.com/flare-im/flare-proto.git "../flare-proto"

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      # 契约由 .proto 生成，构建期需要 protobuf 编译器。
      - name: 安装 protoc
        run: sudo apt-get update -qq && sudo apt-get install -y protobuf-compiler


      # 格式先查：最快，失败得早，省掉后面的编译时间
      - name: rustfmt
        run: cargo fmt --all -- --check

      - name: clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      # nats 不在默认 feature 里，上面这行一行 NATS 代码都编不到。
      - name: clippy（nats 后端）
        run: cargo clippy --workspace --all-targets --features flare-core-messaging/nats -- -D warnings

      - name: test
        run: cargo test --workspace

      # NATS 是生产实际使用的 MQ 后端，但它在默认 feature 之外，
      # `cargo test --workspace` 从来没有编译过它——JetStream stream 配置里
      # 缺 max_bytes 这种会导致全站发消息中断的缺陷因此可以一路进生产。
      - name: test（nats 后端）
        run: cargo test --workspace --features flare-core-messaging/nats

  # 供应链审计与主流程解耦：advisory DB 每天在变，
  # 不该因为上游新披露的 CVE 就让无关的功能 PR 变红。
  audit:
    name: 依赖审计
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      # 本项目是多仓库结构，各仓的 path 依赖指向同级仓库目录。
      # 单仓 checkout 后这些路径不存在，cargo 会直接解析失败，
      # 所以这里按开发时的布局把依赖仓拉到同级位置。
      - name: 拉取同级仓库
        run: |
          BR="${GITHUB_REF_NAME:-main}"
          clone() {
            git clone --depth 1 -b "$BR" "$1" "$2" 2>/dev/null \
              || git clone --depth 1 "$1" "$2"
          }
          clone https://github.com/flare-im/flare-proto.git "../flare-proto"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      # 契约由 .proto 生成，构建期需要 protobuf 编译器。
      - name: 安装 protoc
        run: sudo apt-get update -qq && sudo apt-get install -y protobuf-compiler


      - name: 安装 cargo-audit / cargo-deny
        run: cargo install cargo-audit cargo-deny --locked

      - name: cargo audit（已知漏洞）
        run: cargo audit

      - name: cargo deny（许可 / 来源 / 重复依赖）
        run: cargo deny check
